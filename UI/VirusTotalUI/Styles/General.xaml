<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:VirusTotalUI"
                    xmlns:views="clr-namespace:VirusTotalUI.Views">
  
    <!--Text block styles-->
    <Style TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlockStyle}" x:Key="TB_Defualt">
        <!--<Setter Property="FontFamily" Value="Arial"/>-->
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="Margin" Value="1.5 4"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="VerticalAlignment" Value="Center"/>

    </Style>

    <Style TargetType="TextBlock" BasedOn="{StaticResource TB_Defualt}" x:Key="Tb_Asterisk">
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Margin" Value="0,5,0,-5"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="BaselineOffset" Value="13"/>
        <Setter Property="Text" Value="*"/>

    </Style>

    <!--Image styles-->
    <Style TargetType="Image"  x:Key="ImageStyle">
        <Setter Property="Width" Value="30"/>
        <Setter Property="Height" Value="38"/>
        <Setter Property="Margin" Value="0 0 10 0"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
    </Style>

   

   

</ResourceDictionary>